<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.8.6">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2023-01-04T11:02:56-08:00</updated><id>http://localhost:4000/feed.xml</id><entry><title type="html">Code Shorts: Run R package checks with a shell script</title><link href="http://localhost:4000/blog/2021/08/13/rcheckbash.html" rel="alternate" type="text/html" title="Code Shorts: Run R package checks with a shell script" /><published>2021-08-13T00:00:00-07:00</published><updated>2021-08-13T00:00:00-07:00</updated><id>http://localhost:4000/blog/2021/08/13/rcheckbash</id><content type="html" xml:base="http://localhost:4000/blog/2021/08/13/rcheckbash.html"><![CDATA[<p>In Code Shorts, I describe code usages which I find quirky, interesting, and/or useful. I’ll focus on implementations that could help to improve the working efficiency and quality of life for researchers and coders alike, all while keeping things brief.</p>

<p>While checks are crucial to R package development, running them from command line can quickly become repetitive. I’ve written a shell script, <a href="https://gist.github.com/metamaden/14f6b5717e2934c118ffa9edfa45e765"><code class="highlighter-rouge">rpackagecheck.sh</code></a>, that runs the standard steps to checking an R package. The script uses <code class="highlighter-rouge">R CMD ...</code> to install, build, and check packages with any combination of the three major check types.  This script can help discourage accidents, such as running check on a directory rather than a <code class="highlighter-rouge">.tar.gz</code> file, and ultimately expedite your development workflow.</p>

<h1 id="r-package-checks">R package checks</h1>

<p>If you’ve submitted an R package on CRAN or Bioconductor, you’re familiar with the barrage of checks an R package needs to pass before it can be published. The three most common check types are supported in the <code class="highlighter-rouge">rpackagecheck.sh</code> script detailed below. These are the standard <code class="highlighter-rouge">R CMD CHECK</code>, Bioconductor’s <code class="highlighter-rouge">R CMD BiocCheck</code>, and CRAN’s <code class="highlighter-rouge">R CMD CHECK --as-cran</code>. For specific check requirements, consult the developer documentation for the repo to which you’re submitting (e.g. <a href="https://cran.r-project.org/web/packages/submission_checklist.html">here</a> for CRAN and <a href="https://www.bioconductor.org/developers/package-guidelines/#checkingenv">here</a> for Bioconductor).</p>

<h1 id="running-the-script">Running the script</h1>

<p>When you’re ready to run one or several checks on your package, you can simply run the script from command line.</p>

<p>With the script <a href="https://gist.github.com/metamaden/14f6b5717e2934c118ffa9edfa45e765"><code class="highlighter-rouge">rpackagecheck.sh</code></a> in the current directory and the package folder located at <code class="highlighter-rouge">path/to/package</code>, run:</p>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sh rpackagecheck.sh -p path/to/package  -v -r
</code></pre></div></div>

<p>This example would install the package, build it with vignettes per the <code class="highlighter-rouge">-v</code> flag, then run <code class="highlighter-rouge">R CMD CHECK</code> per the <code class="highlighter-rouge">-r</code> flag.</p>

<h1 id="the-full-script">The full script</h1>

<p>The full <a href="https://gist.github.com/metamaden/14f6b5717e2934c118ffa9edfa45e765"><code class="highlighter-rouge">rpackagecheck.sh</code></a> script is as follows:</p>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/usr/bin/env sh</span>

<span class="c"># Author: Sean Maden</span>
<span class="c">#</span>
<span class="c"># Script to install, build, and check R packages during development. Can perform</span>
<span class="c"># one of 3 check types (R CHECK with and without '--as-cran', or BiocCheck).</span>
<span class="c"># </span>
<span class="c"># Flags:</span>
<span class="c"># -v : Build vignettes (otherwise use `... --no-build-vignettes`)</span>
<span class="c"># -r : Run `R CMD CHECK ...`</span>
<span class="c"># -b : Run `R BiocCheck ...`</span>
<span class="c"># -c : Run CRAN check (e.g. `R CMD CHECK --as-cran ...`)</span>
<span class="c"># </span>
<span class="c"># Returns:</span>
<span class="c"># Builds the package, generates tarball containing the built binaries and check</span>
<span class="c"># logs.</span>

<span class="c">#------------</span>
<span class="c"># parse flags</span>
<span class="c">#------------</span>
<span class="c"># initialize flag variables</span>
<span class="nv">packagepath</span><span class="o">=</span>
<span class="nv">buildvignettes</span><span class="o">=</span>0
<span class="nv">rcheck</span><span class="o">=</span>0
<span class="nv">bioccheck</span><span class="o">=</span>0
<span class="nv">crancheck</span><span class="o">=</span>0

<span class="c"># parse provided flags</span>
<span class="k">while </span><span class="nb">getopts </span>vrbchp: name<span class="p">;</span> <span class="k">do
    case</span> <span class="s2">"</span><span class="nv">$name</span><span class="s2">"</span> <span class="k">in</span>
        <span class="s1">'v'</span><span class="p">)</span>    <span class="nv">buildvignettes</span><span class="o">=</span>1<span class="p">;;</span>
        <span class="s1">'r'</span><span class="p">)</span>    <span class="nv">rcheck</span><span class="o">=</span>1<span class="p">;;</span>
        <span class="s1">'b'</span><span class="p">)</span>    <span class="nv">bioccheck</span><span class="o">=</span>1<span class="p">;;</span>
        <span class="s1">'c'</span><span class="p">)</span>    <span class="nv">crancheck</span><span class="o">=</span>1<span class="p">;;</span>
        <span class="s1">'p'</span><span class="p">)</span>    <span class="nv">packagepath</span><span class="o">=</span><span class="nv">$OPTARG</span><span class="p">;;</span>
        <span class="se">\?</span><span class="p">)</span>     <span class="nb">echo</span> <span class="s2">"Invalid option provided: -</span><span class="nv">$OPTARG</span><span class="s2">"</span> <span class="o">&gt;</span>&amp;2<span class="p">;;</span>
    <span class="k">esac</span>
<span class="k">done</span>

<span class="c">#-------------------------</span>
<span class="c"># package setup for checks</span>
<span class="c">#-------------------------</span>
<span class="c"># check package path existence</span>
<span class="k">if</span> <span class="o">[</span> <span class="o">!</span> <span class="nt">-d</span> <span class="s2">"</span><span class="nv">$packagepath</span><span class="s2">"</span> <span class="o">]</span><span class="p">;</span> <span class="k">then
    </span><span class="nb">echo</span> <span class="s1">'ERROR: invalid package path provided'</span>
    <span class="nb">exit </span>1
<span class="k">fi</span>

<span class="c"># prep the package</span>
R CMD INSTALL <span class="nv">$packagepath</span>

<span class="c"># parse buildvignettes flag</span>
<span class="k">if</span> <span class="o">[</span> <span class="nv">$buildvignettes</span> <span class="o">==</span> 0 <span class="o">]</span><span class="p">;</span> <span class="k">then
    </span><span class="nb">echo</span> <span class="s1">'Skipping vignette builds...'</span>
    R CMD BUILD <span class="nv">$packagepath</span> <span class="nt">--no-build-vignettes</span>
<span class="k">else
    </span><span class="nb">echo</span> <span class="s1">'Building vignettes...'</span>
    R CMD BUILD <span class="nv">$packagepath</span>
<span class="k">fi</span>

<span class="c"># prep the tar path</span>
<span class="nv">packagename</span><span class="o">=</span><span class="si">$(</span><span class="nb">basename</span> <span class="nv">$packagepath</span><span class="si">)</span>
<span class="c"># get the version from description text</span>
<span class="nv">vers</span><span class="o">=</span><span class="si">$(</span><span class="nb">sed</span> <span class="s1">'2q;d'</span> <span class="nv">$packagepath</span><span class="s1">'/DESCRIPTION'</span><span class="si">)</span> 
<span class="nv">vf</span><span class="o">=</span><span class="k">${</span><span class="nv">vers</span>:9<span class="k">}</span>
<span class="nv">tarpath</span><span class="o">=</span><span class="nv">$packagename</span><span class="s1">'_'</span><span class="nv">$vf</span><span class="s1">'.tar.gz'</span> <span class="c"># detect the tar file</span>

<span class="c">#---------------------</span>
<span class="c"># run specified checks</span>
<span class="c">#---------------------</span>
<span class="k">if</span> <span class="o">[</span> <span class="o">!</span> <span class="nv">$rcheck</span> <span class="o">==</span> 0 <span class="o">]</span><span class="p">;</span> <span class="k">then
    </span><span class="nb">echo</span> <span class="s1">'Running `R CMD CHECK`...'</span>
    R CMD CHECK <span class="nv">$tarpath</span>
<span class="k">fi
if</span> <span class="o">[</span> <span class="o">!</span> <span class="nv">$bioccheck</span> <span class="o">==</span> 0 <span class="o">]</span><span class="p">;</span> <span class="k">then
    </span><span class="nb">echo</span> <span class="s1">'Running `R CMD BiocCheck`...'</span>
    R CMD BiocCheck <span class="nv">$tarpath</span>
<span class="k">fi
if</span> <span class="o">[</span> <span class="o">!</span> <span class="nv">$crancheck</span> <span class="o">==</span> 0 <span class="o">]</span><span class="p">;</span> <span class="k">then
    </span><span class="nb">echo</span> <span class="s1">'Running `R CMD CHECK --as-cran`...'</span>
    R CMD CHECK <span class="nt">--as-cran</span> <span class="nv">$tarpath</span>
<span class="k">fi</span>
</code></pre></div></div>

<h1 id="script-anatomy">Script anatomy</h1>

<p>The script handles several flags described in its docstrings:</p>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code># Flags:
# -v : Build vignettes (otherwise use `... --no-build-vignettes`)
# -r : Run `R CMD CHECK ...`
# -b : Run `R BiocCheck ...`
# -c : Run CRAN check (e.g. `R CMD CHECK --as-cran ...`)
</code></pre></div></div>

<p>Each flag, such as <code class="highlighter-rouge">-v</code>, has a corresponding variable, such as <code class="highlighter-rouge">buildvignettes</code>. The default variable values are first defined as:</p>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>packagepath=
buildvignettes=0
rcheck=0
bioccheck=0
crancheck=0
</code></pre></div></div>

<p>Variables <code class="highlighter-rouge">buildvignettes</code>, <code class="highlighter-rouge">rcheck</code>, <code class="highlighter-rouge">bioccheck</code>, and <code class="highlighter-rouge">crancheck</code> act like switches, where a value of 0 = off and 1 = on. The unassigned <code class="highlighter-rouge">packagepath</code> variable will store the user-provided path to the package’s directory.</p>

<p>The flag variables will update according to the user-provided flags. Variable update is handled with <code class="highlighter-rouge">getopts</code> as shown:</p>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>while getopts vrbchp: name; do
    case "$name" in
        'v')    buildvignettes=1;;
        'r')    rcheck=1;;
        'b')    bioccheck=1;;
        'c')    crancheck=1;;
        'p')    packagepath=$OPTARG;;
        \?)     echo "Invalid option provided: -$OPTARG" &gt;&amp;2;;
    esac
done
</code></pre></div></div>

<p>The package path is provided after <code class="highlighter-rouge">-p</code>, e.g. <code class="highlighter-rouge">-p ./my/package/path</code>. This is achieved by setting <code class="highlighter-rouge">packagepath=$OPTARG</code>, where <code class="highlighter-rouge">$OPTARG</code> is the user-provided path to an R package. Providing any additional flags sets the corresponding variables to 1. This ensures the corresponding step is run rather than skipped.</p>

<p>For example, providing a flag <code class="highlighter-rouge">-v -r -b</code> will build the vignettes (per <code class="highlighter-rouge">-v</code>), run <code class="highlighter-rouge">R CMD CHECK</code> (per <code class="highlighter-rouge">-r</code>), then finally run <code class="highlighter-rouge">R CMD BiocCheck</code> (per <code class="highlighter-rouge">-b</code>). Providing the flags in the format <code class="highlighter-rouge">-vrb</code> is also supported.</p>

<p>Before installation, the existence of the provided package path is checked in an if/then statement:</p>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>if [ ! -d "$packagepath" ]; then
    echo 'ERROR: invalid package path provided'
    exit 1
fi
</code></pre></div></div>

<p>If the path is valid, the script proceeds to install and build the package:</p>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>R CMD INSTALL $packagepath
if [ $buildvignettes -gt 0 ]; then
    R CMD BUILD $packagepath
else
    R CMD BUILD $packagepath --no-build-vignettes
fi
</code></pre></div></div>

<p>If the <code class="highlighter-rouge">-v</code> flag was provided, the build includes any vignettes. I included this parameter because I prefer to hold off on building any vignettes until the rest of the package successfully passes checks. This is because vignette builds can take awhile, and errors from vignette builds frequently reflect issues with the package code.</p>

<p>Building the package produces a <code class="highlighter-rouge">tar.gz</code>-compressed file. This file has a predictable file name structure, which the script automatically produces as shown:</p>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code># prep the tar path
packagename=$(basename $packagepath)
# get the version from description text
vers=$(sed '2q;d' $packagepath'/DESCRIPTION') 
vf=${vers:9}
tarpath=$packagename'_'$vf'.tar.gz' # detect the tar file
</code></pre></div></div>

<p>With <code class="highlighter-rouge">sed</code>, the package version is stripped from the DESCRIPTION file in the package directory and used to make the new file name. For instance, if the package name was “newpackage” and the version was “1.0.0”, the new file name would be <code class="highlighter-rouge">newpackage_1.0.0.tar.gz</code>.</p>

<p>Finally, run anywhere from 0 to 3 total checks on the new package file using <code class="highlighter-rouge">R CMD ...</code>:</p>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>if [ ! $rcheck -gt 0 ]; then
    R CMD CHECK $tarpath
fi
if [ ! $bioccheck -gt 0 ]; then
    R CMD BiocCheck $tarpath
fi
if [ ! $crancheck -gt 0 ]; then
    R CMD CHECK --as-cran $tarpath
fi
</code></pre></div></div>

<p>The results are printed to the console and stored in subdirectories of a new directory with the <code class="highlighter-rouge">.Rcheck</code> extension.</p>

<h1 id="conclusions">Conclusions</h1>

<p>This post described the <a href="https://gist.github.com/metamaden/14f6b5717e2934c118ffa9edfa45e765"><code class="highlighter-rouge">rpackagecheck.sh</code></a> shell script, including how to run it from command line and how each part of the script works. Some background about the types of R package checks developers frequently use was also provided. Implementing a repetetive task in a script can save you time and help you to avoid errors, leaving more mental overhead to focus on package development.</p>]]></content><author><name></name></author><category term="blog" /><category term="programming" /><category term="bash" /><category term="shell" /><category term="sh" /><category term="R" /><category term="developers" /><category term="Bioconductor" /><category term="CRAN" /><summary type="html"><![CDATA[How can we more efficiently run R package build using bash?]]></summary></entry><entry><title type="html">Cracking the Monty Hall problem with brute force simulation</title><link href="http://localhost:4000/blog/2020/02/13/montyhall.html" rel="alternate" type="text/html" title="Cracking the Monty Hall problem with brute force simulation" /><published>2020-02-13T00:00:00-08:00</published><updated>2020-02-13T00:00:00-08:00</updated><id>http://localhost:4000/blog/2020/02/13/montyhall</id><content type="html" xml:base="http://localhost:4000/blog/2020/02/13/montyhall.html"><![CDATA[<p>On a game show stage before you wait 3 closed doors, behind which have been deposited 2 goats and 1 prize, respectively. 
You are called on to pick a door to be opened to reveal either a goat or a prize. The host, Monty Hall, then reveals a 
goat behind one of the two remaining unpicked doors. You are then given the option to switch your door selection to the 
final unpicked door before the big reveal. What should you do?</p>

<p>This is the <a href="https://en.wikipedia.org/wiki/Monty_Hall_problem">Monty Hall problem</a>, a kind of logic puzzle involving 
conditional probability. Given that you value prizes over goats and lack prior knowledge about which door the prize is 
behind, it can be readily shown that switching doors <em>always</em> increases your win probability. If you stick with your 
first choice, your success frequency never exceeds 1 of 3 games, while switching increases this to 2 of 3, a pretty 
substantial improvement!</p>

<p>It’s telling that the Monty Hall problem, featured in an <a href="https://en.wikipedia.org/wiki/Let%27s_Make_a_Deal">actual game show</a> 
from the 1960s, still serves as a good brain teaser to this day. Given its simple rules and decision parameters, it’s a 
problem that lends itself to programmatic simulation. In this post, I’ll show how I wrote a simulation function that 
captures the basic (or “classic”) rules of the Monty Hall problem while allowing for exploration of how modifications to the 
underlying rules and conditions can change game outcomes. Hopefully I can inspire you to consider opportunities to tackle 
new problems with simulation strategies.</p>

<p>I’ve deployed the simulation code with a strictly reproducible vignette in the <a href="https://github.com/metamaden/montyhall"><code class="highlighter-rouge">montyhall</code></a> 
R package. Deploying work as an R package can be extremely worthwhile in production level data science projects. In writing the code 
for this package, I’ve knowingly omitting a few best practices for package authorship, in service to expediency and what I consider 
more clearly written code. Note there are <a href="https://cran.r-project.org/submit.html">many</a> 
<a href="https://www.bioconductor.org/developers/package-submission/">great</a> <a href="https://www.bioconductor.org/developers/package-guidelines/">places</a> 
you can and should refer to for learning R package standards and why they matter. The 3 key package functions, <code class="highlighter-rouge">mhgame()</code>, <code class="highlighter-rouge">mhsim()</code>, 
and <code class="highlighter-rouge">getfw()</code>, manage game simulations and return win frequencies across sets of simulated games. These functions only make use of 
base R without added dependencies. I’ve also added several utilities for plots that make use of several stellar R packages, 
including <a href="https://cran.r-project.org/web/packages/ggplot2/index.html"><code class="highlighter-rouge">ggplot2</code></a> and 
<a href="https://cran.r-project.org/web/packages/gridExtra/index.html"><code class="highlighter-rouge">gridExtra</code></a>. Below, I’ll walk through the simulation R code and 
show its use in scripts to investigate the Monty Hall problem in greater depth.</p>

<h1 id="listing-game-steps-and-outlining-code-objectives-with-pseudocode">Listing game steps and outlining code objectives with pseudocode</h1>

<p>Here’s a formulation of the steps in the classic Monty Hall problem, as described above:</p>

<ol>
  <li>Three doors total, behind which 1 has a prize, and the remaining 2 have goats.</li>
  <li>The player picks a door (player decision 1).</li>
  <li>Monty reveals one of the two remaining doors to be a goat.</li>
  <li>The player decides whether to stick with their initial choice or switch to the last unpicked door (player decision 2).</li>
  <li>Game outcome is determined by whether the final player-selected door reveals either a prize (win) or a goat (loss).</li>
</ol>

<p>There are a few key aspects to this formulation. An initial naive formulation might simply state the player picks a door twice, 
with Monty doing something-or-other in between. Instead, I’ve stated the player picks a door (step 2/decision 1) then decides 
whether to switch doors (step 4/decision 2). This distinction is vital because Monty’s step 3 reveal provides new information 
that can help our win chances if we know to heed it in the second player decision. Additionally, randomness is implied in the 
first 3 steps. That is, the prize door is set randomly (step 1), the player picks an initial door randomly (step 2/player 
decision 1), and one third of the time Monty will reveal a goat at random (step 3).</p>

<p>Before we dive into the simulation code, I’ll represent the problem using <a href="https://en.wikipedia.org/wiki/Pseudocode">pseudocode</a> 
to outline tasks the code needs to accomplish. Pseudocode is simply a way of abstracting tasks for programming that has the 
convenience of being language-agnostic. Pseudocode for the classic problem might be something like:</p>

<ul>
  <li>run Monty_Hall_Game:
    <ul>
      <li>get door_indices from 1:ndoors</li>
      <li>assign prize_door</li>
      <li>randomize player_door_index1</li>
      <li>get remaining_doors</li>
      <li>get monty_door_indices up to ndoors - 2</li>
      <li>get player_door_index2 as remaining_door_index</li>
      <li>if player_door_index2 == prize_door, return “win”, else “lose”</li>
    </ul>
  </li>
  <li>run Monty_Hall_Simulation:
    <ul>
      <li>do Monty_Hall_Game up to num_iterations</li>
    </ul>
  </li>
</ul>

<p>This code outlines two functions loosely corresponding to the <code class="highlighter-rouge">mhgame()</code> and <code class="highlighter-rouge">mhsim()</code> functions in the R package. 
In programming, it’s often preferable to break a large problem into smaller sub-problems so that each sub-problem 
solution can be more readily fine-tuned. This reductive coding approach can make debugging and <a href="https://en.wikipedia.org/wiki/Unit_testing">unit testing</a> 
<em>a lot</em> easier, especially as projects increase in complexity. With these conceptual formulations of the problem in mind, 
let’s look at how I wrote the simulation code.</p>

<h1 id="the-simulation-r-code">The simulation R code</h1>

<p>I’ve written 3 functions to help run the game simulations. First, the <code class="highlighter-rouge">mhgame()</code> function runs a single game or 
“game iteration.” Second, <code class="highlighter-rouge">mhsim()</code> executes a series of game iterations that defines a simulation run, up to N = <code class="highlighter-rouge">niter</code> 
total game iterations. Finally, <code class="highlighter-rouge">getfw()</code> takes the output from <code class="highlighter-rouge">mhsim()</code>, a list of game outcome vectors (either “win” or “loss”), 
and returns a single vector of win fractions.</p>

<p>Importantly, <code class="highlighter-rouge">mhsim()</code> <a href="https://en.wikipedia.org/wiki/Automatic_vectorization">vectorizes</a> game simulations with <code class="highlighter-rouge">lapply()</code>. 
Vectorization is a great way to speed up repetitive coding tasks that may otherwise be reflexively implemented in inefficient loops. 
The <code class="highlighter-rouge">lapply()</code> function is a member of the <code class="highlighter-rouge">apply</code> <a href="https://www.rdocumentation.org/packages/base/versions/3.6.2/topics/lapply">family</a> 
of R functions, which have been specialized for different varieties of vectorization tasks. Some other useful ways of speeding up 
your code can include parallelization of tasks with <a href="https://en.wikipedia.org/wiki/Thread_(computing)#Multithreading">multithreading</a>. 
However, note that some parallelization solutions aren’t strictly replicable (e.g. tasks return as they finish in non-determined 
fashion) and may require additional code and dependencies. It’s ideal to tailor the solution complexity to that of its problem. 
For our purposes, running tens of thousands of Monty Hall simulations isn’t memory intensive, and each operation completes in 
about a minute or less.</p>

<p>In <code class="highlighter-rouge">mhsim()</code>, the <code class="highlighter-rouge">niter</code> argument specifies the number of game iterations to simulate, and the <code class="highlighter-rouge">seed</code> argument specifies the seed 
passed to <code class="highlighter-rouge">set.seed()</code>. Setting the seed allows for <em>exact replication</em> of run results even where randomization is implemented. 
I performed randomization steps using the <code class="highlighter-rouge">sample()</code> function.</p>

<p>To show how <code class="highlighter-rouge">mhgame()</code> delivers on the pseudocode tasks above, I’ll describe how it breaks the game into discrete component steps. 
First, the index of the prize door is specified.</p>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>which.prize &lt;- sample(doorseq, nprize)
</code></pre></div></div>

<p>Then the player’s first decision is simulated, where <code class="highlighter-rouge">ndec1</code> is the quantity of doors selected in this step (defaults to 1).</p>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>dec1select &lt;- sample(doorseq, ndec1)
</code></pre></div></div>

<p>Next, Monty reveals a goat. In total, <code class="highlighter-rouge">nr</code> doors are selected from remaining door options in <code class="highlighter-rouge">doorremain1</code>, with a few added 
sanity checks and provisions for extensions such as increasing the prize count. Note if there are 2 valid door options, Monty 
selects one at random, and otherwise reveals the only valid door available.</p>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code># run montyselect
doorremain1 &lt;- doorseq[!doorseq == dec1select] # exclude player first selection
nr &lt;- length(doorremain1) - nrevealdif # calculate the reveal difference
# validate reveal difference value
if(nr &lt; 0 | nr &gt; length(doorremain1) - 1){
  stop("Too many doors specified for Monty to reveal. Increase `nrevealdif`.")
}
if(montyselect == "random"){
  # if more than 1 prize, allow monty to reveal n - 1 prizes
  if(length(which.prize) &gt; 1){
    mdooroptions &lt;- doorremain1
  } else{
    mdooroptions &lt;- doorremain1[!doorremain1 %in% which.prize]
  }
  if(length(mdooroptions) &lt; 2){
    mselect &lt;- mdooroptions
  } else{
    mselect &lt;- sample(mdooroptions, nr)
  }
}
</code></pre></div></div>

<p>Next, we determine the second player decision of whether to switch or stay with their original picked door. The player decision is 
selected from a weighted binomial distribution (details below), where the default is to always switch doors.</p>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code># run decision 2
# exclude monty's doors and decision 1 doors from switch options
doorremain2 &lt;- doorseq[!doorseq %in% c(mselect, dec1select)]
# parse switch likelihood
if(is.numeric(doorswitch) &amp; doorswitch &gt;= 0 &amp; doorswitch &lt;= 1){
  ssvar &lt;- ifelse(doorswitch == 1, "switch", 
                  sample(c(rep("switch", 100*doorswitch), 
                           rep("stay", 100 - 100*doorswitch)), 1))
} else{
  stop("Invalid doorswitch value.")
}
</code></pre></div></div>

<p>The second player decision is then parsed, and the function returns the game outcome (<code class="highlighter-rouge">win</code> or <code class="highlighter-rouge">loss</code>). There’s also a <code class="highlighter-rouge">verbose.results</code> 
option to return the granual details for each game alongside outcomes, which I used for bug squashing.</p>

<h1 id="simulating-the-classic-problem">Simulating the classic problem</h1>

<p>Let’s study the impact of varying the number of simulations and iterations per simulation on the distribution of win frequencies across 
simulations. Again, note that I’ve set the player switch frequency to 100% with the default setting <code class="highlighter-rouge">doorswitch = 1</code>. I started small 
with just 5 simulations of 2 games (10 total games), and increased to 100 (10,000 games) and 1,000 (1,000,000 games) simulations 
and iterations, respectively. To execute the simulations, I iterated over 3 parameter sets and time it with <code class="highlighter-rouge">Sys.time()</code>. I used 
a <code class="highlighter-rouge">for</code> loop to iterate over the indices of the 3-value parameter vectors where indices are used to retrieve parameters for each run.</p>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code># parameter sets
simv &lt;- c(5, 100, 1000)
iterv &lt;- c(2, 100, 1000)
lr &lt;- list()
t1 &lt;- Sys.time()
for(s in 1:length(simv)){
  runname &lt;- paste0(simv[s], ";", iterv[s])
  lr[[runname]] &lt;- getfw(nsimulations = simv[s], niterations = iterv[s])
}
tdif &lt;- Sys.time() - t1
</code></pre></div></div>

<p>The 3 runs completed in about 27 seconds. With so few iterations and simulations in the first run, there’s huge variance in the 
win fraction (standard deviation of 0.45). Increasing iterations and simulations each to 100 already shows the distribution 
converging on the expected win frequency of 0.66. Further increase to 1,000 simulations and iterations results in a more clearly 
normal distribution with much tighter standard deviation of 0.01.</p>

<p>Let’s now plot the win frequency distributions across the 3 runs. Note I’ve stored the run info (number of simulations and iterations 
per run) in the list names, and we can unpack these with <a href="https://en.wikipedia.org/wiki/Regular_expression">regular expressions</a> using 
<code class="highlighter-rouge">gsub()</code> for the respective plot titles. I’ll use <code class="highlighter-rouge">par</code> to manage the plot output and formatting, where <code class="highlighter-rouge">nrow = c(1, 3)</code> specifies 
the plot output conforms to a matrix of 1 row and 3 columns, and <code class="highlighter-rouge">oma = c(3, 3, 3, 1)</code> adds outer margin whitespace for axis labels. 
I’ll remove redundant axis labels for each plot and add these back with <code class="highlighter-rouge">mtext()</code>.</p>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>png("mh_3runs.png", width = 10, height = 4, units = "in", res = 400)
# format image output
par(mfrow = c(1, 3), oma = c(3, 3, 3, 1))
for(r in 1:length(lr)){
  rdat &lt;- lr[[r]]
  # get plot title info
  rname &lt;- names(lr)[r]
  simr &lt;- gsub(";.*", "", rname)
  iterr &lt;- gsub(".*;", "", rname)
  pmain &lt;- paste0(simr," simulations\n", iterr, " iterations")
  # add run histogram to image output
  hist(rdat, main = pmain, xlab = "", ylab = "")
}
# add outer axis labels
mtext("Win Frequency", side = 1, outer = T)
mtext("Number of Simulations", side = 2, outer = T)
dev.off()
</code></pre></div></div>

<p><img src="https://raw.githubusercontent.com/metamaden/montyhall/master/plots/mh_3runs.png" align="center" alt="drawing" width="1800" /></p>

<p><strong>Figure 1.</strong> Composite histograms of Monty Hall simulation runs using classic parameters. At left is smallest run (5 simulations of 2 
games, 10 games total), middle is intermediate run (100 simulations and games, 10,000 total games), right is largest run (1,000 simulations 
and games, 1,000,000 games total).</p>

<p>If you prefer to be more precise about the increase in distribution normality, we can apply the 
<a href="https://en.wikipedia.org/wiki/Shapiro%E2%80%93Wilk_test">Shapiro-Wilk Normality test</a>
with <code class="highlighter-rouge">shapiro.test()</code> to test the null hypothesis that data were drawn from a normal distribution.</p>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code># run normality tests
st1 &lt;- shapiro.test(lr[["5;2"]])$p.value
st2 &lt;- shapiro.test(lr[["100;100"]])$p.value
st3 &lt;- shapiro.test(lr[["1000;1000"]])$p.value
</code></pre></div></div>

<p>With increased simulations and iterations, our p-value increased from 0.05 in the first and smallest run to 0.58 in the third and 
largest run. In other words, confidence decreases for rejecting the null hypothesis (normality) or accepting the alternative 
hypothesis (non-normality) as the underlying distributions converge to approximate normality.</p>

<h1 id="bending-the-game-rules">Bending the game rules</h1>

<p>I’ve written the simulation code to execute the Monty Hall problem with its classic characteristics by default, while allowing for 
modification of certain game rules and conditions. For our purposes, these classic parameters include randomization of the first 3 
steps and that Monty reveals all but 1 door between player decisions. Changing other game conditions and studying the impact on win 
frequency distributions can help us better understand how the game ticks. In the process, visualizing our simulation results is a 
compelling way of reinforcing the notion that switching always increases win chances under the classic game rules.</p>

<p>The first condition I’ll explore is door quantity, which can be set with the <code class="highlighter-rouge">ndoors</code> argument to <code class="highlighter-rouge">mhsim()</code>. This value then gets 
passed to <code class="highlighter-rouge">mhgame()</code>. In practice this simply sets the <code class="highlighter-rouge">doorseq</code> vector of door indices to be of length <code class="highlighter-rouge">ndoors</code>, and subsequent 
steps proceed as normal.</p>

<p>Next, we can vary the player switch frequency (decision 2). The default setting <code class="highlighter-rouge">doorswitch = 1</code> means the player switches 100% of 
the time, and this can be decreased to some decimal between 0 and 1. The player decision (switch or stay) is determined by randomly 
selecting from a weighted binomial distribution defined by the argument. So if <code class="highlighter-rouge">doorswitch = 0.2</code>, the player decision is drawn 
from a distribution where 20% of options are “switch” and (100 - 20 = ) 80% of options are “stay”. Again, setting this argument 
allows other game steps to run as normal.</p>

<h1 id="increasing-door-counts-and-visualizing-the-mnemonic-device">Increasing door counts and visualizing the mnemonic device</h1>

<p>Besides rote memorization, a useful <a href="https://en.wikipedia.org/wiki/Mnemonic">mnemonic device</a> to intuit or re-derive that we should 
<em>always</em> switch doors is to simply increase the number of doors while preserving the other default settings/game rules. Maybe we’re 
unsure if switching doors will increase our win chances with 3 doors. But if there’s instead 100 doors and Monty reveals goats behind 
98 of them, it’s much clearer that switching will increase our chances of winning. As enumerated, we can quantitatively simulate 
outcome results from increasing the door quantity. Further, visualizing these results effectively can reinforce the intuition gained 
from this many-doors mnemonic device.</p>

<p>Let’s now generate and time the results of running 100 simulations of 100 classic games. I’ll vary <code class="highlighter-rouge">ndoors</code> from 3 to 103 by 10, with 
otherwise default settings (including that the player always switches their door choice).</p>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code># get win frequencies from varying ndoors
simi = 100; iteri = 100
ndoorl &lt;- seq(3, 103, 10)
seedl &lt;- seq(1, 100, 1)
lnd &lt;- list()
t1 &lt;- Sys.time()
for(nd in ndoorl){
  fw &lt;- getfw(simi, iteri, nd)
  lnd[[paste0(nd)]] &lt;- fw
}
tdif &lt;- Sys.time() - t1
# store the reference plot
pref &lt;- getlineplot(lnd, ptitle = "Canonical rules, varying doors")
</code></pre></div></div>

<p>All runs completed in about 5 seconds. Let’s visualize results in a few different ways. First, I’ll generate 
<a href="https://en.wikipedia.org/wiki/Violin_plot">violin plots</a>, a powerful way of showing density distributions. 
Because violin plots are relatively distribution-agnostic (e.g. no need to assume or imply distribution normality), they are 
often superior to traditional boxplots. Next, I’ll use overlapping density plots or “ridge plots.” These sometimes also 
called “joyplots” in homage to the iconic cover of Joy Division’s Unknown Pleasures record 
(<a href="https://en.wikipedia.org/wiki/Unknown_Pleasures#Artwork_and_packaging">awesome!</a>). Finally, I’ll show line plots of run 
means with confidence boundaries. Where violin and ridge plots are relatively faithful representations of the underlying 
distributions, line plots use space a bit more efficiently and provide key characteristics of the plotted distributions.</p>

<p>To generate the plots, I’ve wrapped code into the utility functions <code class="highlighter-rouge">getggdat()</code> (format data for violin and ridge plots), 
<code class="highlighter-rouge">getlinedat()</code> (format data for line plots), <code class="highlighter-rouge">getlineplot()</code> (generate line plot object), <code class="highlighter-rouge">getprettyplots()</code> 
(generate composite of 3 ggplot2 plot types). I won’t explain these in depth here, but you may find these functions 
useful as a starting point towards a generalizable method to make these sorts of plots. This code makes use of some 
supremely awesome R packages that most data scientists will find useful, including 
<a href="https://cran.r-project.org/web/packages/ggplot2/index.html"><code class="highlighter-rouge">ggplot2</code></a> (powerful plotting functions and meta syntax), 
<a href="https://cran.r-project.org/web/packages/gridExtra/index.html"><code class="highlighter-rouge">gridExtra</code></a> (managing plot outputs and composite plotting), 
and <a href="https://cran.r-project.org/web/packages/ggridges/index.html"><code class="highlighter-rouge">ggridges</code></a> (ridge plot options).</p>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>png("mh_ndoors_3plots.png", width = 10, height = 4, units = "in", res = 400)
getprettyplots(lnd, "Varying door count")
dev.off()
</code></pre></div></div>

<p><img src="https://raw.githubusercontent.com/metamaden/montyhall/master/plots/mh_ndoors_3plots.png" align="center" alt="mh_ndoors_3plots" width="1800" /></p>

<p><strong>Figure 2.</strong> Three ways to visualize simulated win fraction distributions across door various quantities. Left, 
violin plots. Middle, ridge plots. Right, line plot of distribution means (lines) and standard deviation confidences (grey ribbon).</p>

<p>Thus we have 3 ways of visualizing the win frequency distribution increase following increased door quantity. 
Interestingly, as door quantity increases, the standard deviation seems to contract slightly after the means 
show an asymptote, which reflects that win frequency increase becomes both higher and more certain at higher door quantities.</p>

<p>I’ve allowed for two types of line plot overlays with the <code class="highlighter-rouge">ribbontype</code> argument. This allows for 2 types of 
confidence visualizations (the grey-colored ribbon overlay). Confidences can use either the standard deviation 
(if <code class="highlighter-rouge">sd</code>, the default), or the minimum and maximum win frequencies observed (if <code class="highlighter-rouge">minmax</code>). Let’s compare these below.</p>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pclassic1 &lt;- getlineplot(lnd, ptitle = "Std. dev. overlay", ribbontype = "sd")
pclassic2 &lt;- getlineplot(lnd, ptitle = "Min. max. overlay", ribbontype = "minmax")

png("mh_2lineplots.png", width = 5, height = 3, units = "in", res = 400)
grid.arrange(pclassic1, pclassic2, top = "Ribbon overlay comparison", ncol = 2)
dev.off()
</code></pre></div></div>

<p><img src="https://raw.githubusercontent.com/metamaden/montyhall/master/plots/mh_2lineplots.png" align="center" alt="mh_2lineplots" width="900" /></p>

<p><strong>Figure 3.</strong> Line plot comparison showing 2 ways of visualizing win fraction distributions with door quantity. 
Left, grey confidence ribbon calculated using standard deviation. Right, confidences calculated from observed 
distribution minima and maxima.</p>

<p>Again, I’ll tend to use <code class="highlighter-rouge">sd</code> as it’s more useful to describe the underlying win fraction distributions being plotted.</p>

<h1 id="what-if-the-player-doesnt-always-switch">What if the player doesn’t always switch?</h1>

<p>Next, let’s observe the impact of changing the player switch frequency, or how often the player switches (player decision 2) 
from their initial door selection (decision 1). I’ll do this by varying the <code class="highlighter-rouge">doorswitch</code> argument, which parses player 
choice for each iteration from a weighted binomial distribution. I’ll also covary the door quantity with switch frequency, 
and observe outcome changes across both.</p>

<p>I’ll run 10 simulations varying the switch frequency from 0% to 100% in increments of 10%, and varying the door quantity 
as above (3 to 103 by 10). I’ll then store the results in <code class="highlighter-rouge">ldat</code> and the plots in <code class="highlighter-rouge">plist</code>. For the results plot, 
I’ve set identical x- and y-axis ranges in <code class="highlighter-rouge">getlineplot</code> to aid with visual comparison.</p>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code># get fwin dist across ndoors
plist &lt;- list()
sfreq &lt;- seq(0, 1, 0.1)
t1 &lt;- Sys.time()
ldat &lt;- list()
for(s in sfreq){
  simi = 100; iteri = 100
  ndoorl &lt;- seq(3, 103, 10)
  seedl &lt;- seq(1, 100, 1)
  lnd &lt;- list()
  for(nd in ndoorl){
    fw &lt;- getfw(simi, iteri, nd, doorswitch = s)
    lnd[[paste0(nd)]] &lt;- fw
  }
  plist[[paste0(s)]] &lt;- getlineplot(lnd, ptitle = paste0("S.F. = ", s),
                                    xlim = c(0, 100), ylim = c(0, 1),
                                    xlab = "", ylab = "")
  ldat[[paste0(s)]] &lt;- lnd
  # message(s)
}
tdif &lt;- Sys.time() - t1
</code></pre></div></div>

<p>All runs completed in about 1 minute. The composite plot can now generated from <code class="highlighter-rouge">plist</code>.</p>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>png("mh_switchfreq.png", width = 10, height = 6, units = "in", res = 400)
grid.arrange(plist[[1]], plist[[2]], plist[[3]],
             plist[[4]], plist[[5]], plist[[6]],
             plist[[7]], plist[[8]], plist[[9]],
             plist[[10]],
             ncol = 5, 
             bottom = "Number of Doors", left = "Win Fraction")
dev.off()
</code></pre></div></div>

<p><img src="https://raw.githubusercontent.com/metamaden/montyhall/master/plots/mh_switchfreq.png" align="center" alt="mh_switchfreq" width="1500" /></p>

<p><strong>Figure 4.</strong> Composite line plots of ten simulation sets varying player switch frequency/fraction and door counts. 
Top row, first run to fifth run varying switch frequnecy from 0% to 40%. Bottom row, sixth run to final run varying 
switch frequency from 50% to 90%.</p>

<p>Across run sets of each door switch frequency, there’s a clear transition from an approximate negative power 
function (e.g. x ^ -1, top leftmost plot), to something approaching a fractional power function (e.g. x ^ 1/2, 
bottom rightmost plot). Note the win fraction only starts to show improvement with door quantity increase when the 
switch frequency is greater than 50% (bottom, second from leftmost plot), and that win fraction changes for a given 
switch frequency tend to always form asymptotes.</p>

<p>Increasing the switch frequency under classical rules should show progressive win fraction increases. Let’s 
generate and visualize the simulation results for this. I’ll appropriate my <code class="highlighter-rouge">getlineplot()</code> function, but note 
that it can be better to explicitly handle different axis variables (e.g. <code class="highlighter-rouge">ndoors</code> and <code class="highlighter-rouge">doorswitch</code> here) with 
discrete code.</p>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sfreq &lt;- seq(0, 1, 0.1)
lnd &lt;- list()
for(s in sfreq){
  simi = 100; iteri = 100
  seedl &lt;- seq(1, 100, 1)
  fw &lt;- getfw(simi, iteri, doorswitch = s)
  lnd[[paste0(s)]] &lt;- fw
}
png("mh_switchfreq_classicrules.png", width = 4, height = 4, res = 400, units = "in")
getlineplot(lnd, ptitle = "Win Freq. by Switch Freq.", 
            xlim = c(0, 1), ylim = c(0, 1), 
            xlab = "Switch frequency")
dev.off()
</code></pre></div></div>

<p><img src="https://raw.githubusercontent.com/metamaden/montyhall/master/plots/mh_switchfreq_classicrules.png" align="center" alt="mh_switchfreq_classicrules" width="500" /></p>

<p><strong>Figure 5.</strong> Simulation results from varying switch frequency using only classic Monty Hall rules/parameters (function defaults).</p>

<p>The resulting plot shows a clear linear win fraction increase with switch frequency, maxing out at the now-familiar 
mean of about 0.667.</p>

<h1 id="animating-results-plots">Animating results plots</h1>

<p>In data science, more tools in our toolkit means more options for tackling future problems. Here, I’ll illustrate 
how the <code class="highlighter-rouge">ggplot2</code> meta syntax can be readily leveraged to generate useful plot animations. I’ve written the 
<code class="highlighter-rouge">getprettygifs()</code> function using the <a href="https://cran.r-project.org/web/packages/gganimate/index.html"><code class="highlighter-rouge">gganimate</code></a> 
and <a href="https://cran.r-project.org/web/packages/magick/index.html"><code class="highlighter-rouge">magick</code></a> packages and helpful code provided 
<a href="https://github.com/thomasp85/gganimate/wiki/Animation-Composition">here</a> to generate animated gifs of some of 
the plots above. I’ll generate these gifs using the results stored in the <code class="highlighter-rouge">ldat</code> list.</p>

<div class="highlighter-rouge"><div class="highlight"><pre class="highlight"><code>getprettygif(ldat[[11]], plottype = "composite_ndoors", gifname = "mh_ndoors.gif")
getprettygif(ldat, plottype = "lineplots_doorswitch", gifname = "mh_switchfreq.gif")
</code></pre></div></div>
<p><img src="https://raw.githubusercontent.com/metamaden/montyhall/master/plots/mh_ndoors.gif" alt="https://raw.githubusercontent.com/metamaden/montyhall/master/plots/mh_ndoors.gif" /></p>

<p><strong>Figure 6.</strong> Animation of win fraction distributions from varying door quantity with otherwise classic rules. 
Left is violin plot animation, right is line plot animation. Title describes the nearest frame displayed.</p>

<p><img src="https://raw.githubusercontent.com/metamaden/montyhall/master/plots/mh_switchfreq.gif" alt="https://raw.githubusercontent.com/metamaden/montyhall/master/plots/mh_switchfreq.gif" /></p>

<p><strong>Figure 7.</strong> Animation of line plots showing win fraction distributions from varying player switch frequency 
and door quantities. Title describes the nearest frame displayed.</p>

<h1 id="conclusions-and-analysis-extensions">Conclusions and analysis extensions</h1>

<p>I’ve explored simulations of the Monty Hall problem using a brute force approach. By exploring changes in win 
frequency across varying problem conditions, I’ve proven that switching doors will tend to increase player win 
frequency. I quantitatively investigated how always switching doors improves win frequency as door quantity 
increases, and visualized results to reinforce intuition from the many-doors mnemonic device. I also investigated 
how player switch frequency modifies win frequency gain or loss across door quantities. This showed recurrent 
asymptotic win frequency gain or loss, and how win frequency starts to improve with increase door quantity when 
the player switches over half the time.</p>

<p>This brute force simulation approach is one of many possible ways of sloving and exploring the Monty Hall problem. 
An alternate approach implementing <a href="https://en.wikipedia.org/wiki/Bayesian_statistics">Bayesian models</a> could lead 
to further insights. There are several other game conditions that could also be explored, such as increasing the 
total number of doors with prizes. Ultimately, I hope this investigation provided some useful code and a framework 
for investigating new problems through simulation.</p>]]></content><author><name></name></author><category term="blog" /><category term="simulation" /><category term="modeling" /><category term="programming" /><category term="R" /><category term="ggplot2" /><category term="gganimate" /><category term="monty_hall" /><category term="algorithms" /><category term="statistics" /><summary type="html"><![CDATA[Can simulations help us to understand and solve the Monty Hall problem?]]></summary></entry><entry><title type="html">Mushroom edibility with data science and machine learning – my ML final project</title><link href="http://localhost:4000/blog/2018/12/25/machine-learning-shrooms.html" rel="alternate" type="text/html" title="Mushroom edibility with data science and machine learning – my ML final project" /><published>2018-12-25T00:00:00-08:00</published><updated>2018-12-25T00:00:00-08:00</updated><id>http://localhost:4000/blog/2018/12/25/machine-learning-shrooms</id><content type="html" xml:base="http://localhost:4000/blog/2018/12/25/machine-learning-shrooms.html"><![CDATA[<p>This past Fall quarter, I completed my first college-level machine learning course. 
I hope you will enjoy this discussion of my final project report, entitled, if you can forgive the pun, <em>Shrooms and Tunes</em>. 
You can find the report in its entirety in my <a href="https://github.com/metamaden/machine-learning">machine learning repo</a>.</p>

<p>My report analyzes the <a href="https://archive.ics.uci.edu/ml/datasets/mushroom">UCI Mushrooms dataset</a>. 
There are a lot of example analyses looking at this dataset over the years, including several fairly recent analyses 
(e.g. <a href="https://rpubs.com/soumya2g/CUNY-Coursework">here</a>, <a href="http://inmachineswetrust.com/posts/mushroom-classification/">here</a>, 
and <a href="http://rstudio-pubs-static.s3.amazonaws.com/293253_357dc3b3d3de44c8896039a9985674d4.html">here</a>) that use similar 
strategies to mine. So consider my report a throwing of my hat into the ring. With this post, I want to take this 
opportunity to write at a high level about the process of conducting a data science experiment, including my reasoning 
about statistics and experimental design, how I interpret model predictive performances, and how I form a narrative 
around quantitative facts about the data. Before I continue though, here is a quick disclaimer from my report’s discussion, 
facetiously titled “What Can I Eat?”:</p>

<blockquote>
  <p>“Vitally, the UCI Mushroom dataset is simulated based on real species descriptions, and it should not be considered a 
substitute for real field data from biological specimens! As such, conclusions from this analysis should in no way be 
used to inform any actual decisions on what type of mushrooms it is ok to consume!”</p>
</blockquote>

<p>I won’t pretend at being a mycologist here, or a mushroom expert of any ilk, though I do possess some background in 
field biology. In fact, it’s this background that compells me to highlight the synthetic nature of the data I’m using. 
Perhaps my findings could be used to test new hypotheses about real specimens. But my ultimate intent is not to get 
pedantic about mushrooms. I rather hope to demonstrate how to think quantitatively, skeptically, and empirically 
about an interesting data analysis problem. No more, and no less.</p>

<p>My experiment design tested models using one of three algorithm classes: 1. random forest; 2. support vector machines 
(SVM); or 3. neural networks. For this, I applied the R packages 
<a href="https://cran.r-project.org/web/packages/randomForest/index.html">randomForest</a>, 
<a href="https://cran.r-project.org/web/packages/e1071/index.html">e1071</a>, and 
<a href="https://cran.r-project.org/web/packages/keras/index.html">keras</a>, respectively, all of which I found to be supremely 
accessible and time-efficient. I may post a strictly-replicable markdown essay that recreates my report, 
with embedded code and annotations. For now, I will proceed to discuss my findings and thought process.</p>

<p>Before analysis, I needed to preprocess the mushrooms data. It takes the form of typical field data, 
including 25 categorical variable types pertaining to various aspects of mushroom species phenotype 
(e.g. stalk surface, odor type, spore print color, etc.). Since most machine learning algorithms 
require numeric inputs, I recoded variables using one-hot encoding, or “OHE.” This approach is used to 
transform a factor of more than two levels into some number of new binary numeric variables, equal to 
the number of levels in the original variable. For instance, if we pretend a variable “spot color” 
can take possible states of “black”, “brown”, or “none”, OHE would re-code this variable into three 
new ones: 1. black spot color; 2. brown spot color; or 3. no spot color, where each new variable can 
be either 0 (no/False, etc.) or 1 (yes/True, etc.). OHE has the caveat of greatly increasing dimensionality, 
which is typically not desirable because it increases risk of model overfitting. That is, the data becomes 
too granualar, and real observable effects are too divided amongst too-similar and/or confounded variables. 
Here, the initial 25 categorical variables were converted to 112 binary numeric variables. While a 
substantial dimensionality increase, 112 is actually not a huge number of variables in and of itself, 
especially considering other types of data often tackled with machine learning, and the increased 
risk of overfitting is thus somewhat mitigated here.</p>

<p>After recoding the entire dataset, I divided it equally and randomly into training and validation subsets, 
conserving variable state frequency (0/”no” or 1/”yes”) and instance category frequency (“edible” or “poisonous”) 
as much as possible between subsets.</p>

<p><img src="https://raw.githubusercontent.com/metamaden/metamaden.github.io/master/_posts/media/varimp_rforest.jpg" alt="varimp_rforest.jpg" /> 
<strong>Figure 1.</strong> Relative variable importance in random forests model test (N = 5,000 trees).</p>

<p>I then fitted and tested each model on the preprocessed data. It was surprising to observe substantial 
discrepancy in model predictive performance across the different algorithms. Testing various 
hyperparameter sets with random forest or SVM, I observed uniformly high performance, especially 
when compared to virtually every neural network model I tested. There were slight performance differences 
suggesting increased performance with either increased tree count (random forest, with ntrees from 10 to 5,000) 
or a variable weight filter (SVM, retaining the top 50% highest-weighted variables and using a linear kernel). 
These high-performing models converged on distinct important variable sets (Figure 1, above, and Figure 2, below). 
But with neural networks, overall performance was generally erratic across 50 epochs of training, and I had to 
consider evidence of overfitting (e.g. decline in performance on validation data over training epochs, Figure 3, below), 
to determine which network hyperparameter set was most promising.</p>

<p>There could be several reasons for this overall subpar neural network performance. Neural networks may 
generally be less suited than alternative algorithms for training using binary data. It’s also possible 
that I didn’t encounter the optimum network topology or strategy for these particular data, despite 
testing over 10 different kinds varying hidden layer quantity, nodes per hidden layer, and/or batch size. 
Finally, it’s possible neural networks are more susceptible to noisiness in the data. This point relates 
to an interesting property of the UCI Mushrooms data, that the poisonous instances combine species of 
unknown edibility with known poisonous species, presumably to help increase the effective size of the dataset. 
Unfortunately, I could not find a form of the dataset designating instances of unknown edibility, and so 
could not diagnose the impact of unknown species instances directly.</p>

<p><img src="https://raw.githubusercontent.com/metamaden/metamaden.github.io/master/_posts/media/varimp_svm.jpg" alt="varimp_svm.jpg" />
<strong>Figure 2.</strong> Relative variable weight/importance of top-weighted variables in support vector machine (SVM) 
model test (using linear kernel and top-50% variable weight filter).</p>

<p>Too often in science, researchers operate as though there is an unspoken one-test-type limit for any given 
analysis or experiment. Coincidentally, the favored test type tends to coincide with whatever test(s) the 
researcher happens to be most familiar with. This inadvertent myopia can mean an important point gets overlooked: 
recurrence of a conclusion from different tests using fundamentally distinct algorithm types (and, subsequently, 
distinct assumptions, conditions, and limitations), can itself provide a form of consilience. That is, a 
convergence of knowledge from multiple disparate angles. This point can be leveraged to limit false positive 
or misleading results, with the caveat that any test introduces its own chances for error. Here, I was interested 
in the extent of consilience across the three types of algorithms for identifying important mushroom traits that 
predict edibility.</p>

<p><img src="https://raw.githubusercontent.com/metamaden/metamaden.github.io/master/_posts/media/nnepoch.jpg" alt="nnepoch.jpg" />
<strong>Figure 3.</strong> Performance of neural network over 50 epochs (with 1 hidden layer, 5 hidden nodes per layer, and 
batch size equal to training set sample size).</p>

<p>For SVM and random forest algorithms, relative variable importance is readily quantified and has intelligible 
meaning (Figures 1 and 2, above). Considering relative variable performance across models of each of these classes, 
there were a handful of variables pertaining to odor and color pattern that were recurrently important (e.g. 
foul creosote odor predict poisonous types, none, anise, and almond predict edible types, etc). According to 
the dataset’s documentation, these variables were also among those considered important form rules derived from 
prior analyses over the years (e.g. one rule states “odor NOT (almond, anise or none)” predicts poisonous cases 
with high accuracy, which is consistent with my findings).</p>

<p>To determine relative variable importance in neural networks, I had to get creative. By design, neural networks 
allow for mixing of input node effects, sometimes in complex and counter-intuitive ways. This precludes straightforward 
quantification of individual variable importance for network prediction, or output layer activation. But in a slightly 
counter-intuitive way, we can indirectly get at a variable’s importance… by assessing the impact of its very absence! 
We should expect a greater negative impact to model performance when an important variable, or subset of important 
variables, is excluded, as opposed to when random variables are excluded. So I considered a handful of the most 
recurrent and putatively important variables identified from random forest and SVM models (Table 1, below). 
Working with the best-performing neural network model, assessed the deleterious impact to model performance after 
effectively excluding the important variable set by manually changing all instance values to 0. I also excluded a 
random subset of the same number of variables from the data and reassessed model performance in a separate test. 
Sure enough, exclusion of the putative important variables showed a more deleterious effect on model performance 
(e.g. accuracy decreased from 90% to 73%, versus 83%), providing further support of these variable’s importance 
for determining mushroom edibility.</p>

<p><strong>Table 1.</strong> Top Mushroom Edibility Traits and Species Examples
<img src="https://raw.githubusercontent.com/metamaden/metamaden.github.io/master/_posts/media/shroomtable.jpg" alt="shroomtable.jpg" /></p>

<p>More often than not, a satisfying investigation turns up and motivates many more interesting questions than it answers, 
and this analysis was no exception. The problem of isolating physical traits that determine mushroom edibility presents 
some interesting opportunities for data science and machine learning. In my report, I explored one approach using 
synthesized specimen data and a range of different models and algorithm types (see Table 1 for several of the most 
important species traits for determining edibility, according to my analyses). I applied a binarized edibility 
conditional (e.g. “eidble” or “poisonous”), but if we consult an actual <a href="http://www.svims.ca/council/Lepiot.htm">field guide</a>, 
we readily find greater natural variety and ambiguity of “edibility” states observed among wild mushrooms. Notably, some 
species are just potentially poisonous (e.g. “potentially deadly—has amanitins”), and some even have conflicting evidence 
(e.g. “‘edible’ in some European field guides; a look-alike may be lethal.”). This highlights an important implicit assumption, 
which is that edibility traits coincide with species traits. Given the above, there is good reason to suspect species-identifying 
traits don’t actually correlate well with edibility-determining traits, at least not for all wild North American mushrooms. 
Stepping back, we can consider other potentially useful analyses. It would be fascinating, for instance, to train a visual 
algorithm to extract important image features from photos of real mushrooms in the field, as they might appear to a real-life 
field mycologist or mushroom hunter, and to then compare extracted visual traits to visual traits found to differentiate 
mushroom species here (e.g. “green spore color”, “pendant ring type”, “broad gill size”, etc.).</p>]]></content><author><name></name></author><category term="blog" /><category term="machine_learning" /><category term="artificial_intelligence" /><category term="keras" /><category term="R" /><category term="Python" /><category term="e1071" /><category term="randomForest" /><category term="algorithm" /><category term="computer_science" /><category term="biology" /><category term="computation" /><summary type="html"><![CDATA[Can machine learning tell us which mushrooms are safe to eat?]]></summary></entry><entry><title type="html">My 2018 proposal for the Better Scientific Software Fellowship</title><link href="http://localhost:4000/blog/2018/11/01/bssw-proposal.html" rel="alternate" type="text/html" title="My 2018 proposal for the Better Scientific Software Fellowship" /><published>2018-11-01T00:00:00-07:00</published><updated>2018-11-01T00:00:00-07:00</updated><id>http://localhost:4000/blog/2018/11/01/bssw-proposal</id><content type="html" xml:base="http://localhost:4000/blog/2018/11/01/bssw-proposal.html"><![CDATA[<p>I wanted to share my proposal for the <a href="https://bssw.io/fellowship">2018 Better Scientific Software (BSSw) Fellowship </a>. BSSw aims to increase and preserve integrity and standards for publishing computer code in science, and their fellowship program recognizes and supports advocates of this cause. You may or may not be aware that we currently lack standard ways of referencing published code in science as independently citable units. Furthermore, vital source code for experiments can be distributed in many places, including supplemental materials sections behind paywalls, personal websites that may become inaccessible or go offline over time, and repositories on GitHub or elsewhere that may not include inherent and persistent identifiers. I propose using an autocompilation technology to aggregate published scientific computer code and code metadata into a new database, called Pubsrc. This will enable novel assessments of scientific code use, including automatic generation of dependency usage networks, tracking the impact of newly discovered software bugs throughout research, and making scientific code independently citable. I hope you enjoy reading my proposal, and please share or tweet this post if you support this cause.</p>

<hr />

<p>Section: Experience</p>

<p>Prompt: Describe your work relevant to scientific software.</p>

<p>My Response: My main scientific mission is to reduce friction and latencies in data analysis. In my experience practicing bioinformatics, I grappled with the many difficulties surrounding analysis of genomics data, so I designed and implemented new scientific software to address them. I worked for 3 years in Prof. Bill Grady’s clinical epigenetics lab at Fred Hutch in Seattle, where I cultivated expertise in the epigenetics and molecular bases of gastroesophageal cancers, working my way from intern to Data Analysis Assistant. I collaborated with physicians, wet lab researchers, and fellow science analysts. My experiences led me to design and publish preprocessing and analysis workflows for methylation arrays, which probe chemical modifications to DNA that affect gene expression. I also developed a series of tools and resources to aid other researchers (see https://github.com/metamaden, in particular the methyPre, cgmappeR, and cgageR repositories). I further made intellectual contributions at an NIH hackathon event, where I pushed commits to the open-source PubCode project, a resource for indexing published scientific code (see project repository at https://github.com/NCBI-Hackathons/PubCode). These past efforts inspired the present BSSw fellowship proposal, as described below.</p>

<p>Prompt: Describe your background and experience relevant to being a BSSw Fellow.</p>

<p>My Response: The BSSw Fellowship supports preserving code functionality with proper versioning and documentation, and creating resources that aid researchers in doing code-based science effectively. I have extensive professional experience as a bioinformatician working with public data repositories and scientific code. This has enabled me to tackle systemic problems around code access, publication, and reuse for reproducing findings in computational science. At an NIH hackathon event, I contributed to PubCode, which aimed to index scientific software similar to how PubMed indexes journal articles in biomedical science. PubCode would have archived and made citable scientific code used in research papers. This is important: if a developer took down their code or its associated executables, portions of papers using them could become irreproducible. Moreover, when no publication is associated with software, authors may cite a GitHub or Bitbucket repository, or some other website lacking a persistent digital object identifier (DOI). This distributes citations across multiple resources, making it challenging to find all usages of a software package in the literature. Finally, when a publication uses new code, that code is often not released as standalone software. This means the code is not widely identified as an independently citable object. Bureaucratic issues at the NIH unfortunately prevented PubCode from realizing its full potential, and the project I propose here will subsume its ideas.</p>

<p>Section: Proposed work and potential impact</p>

<p>Prompt: What would you do as a BSSw Fellow?</p>

<p>My Response: The problems I seek to solve are: (1) insufficient attribution of scientific code to its authors, because no resource tracks usages of published scripts or software tools (independent from any associated journal article) across the scientific literature; (2) bugs in software can invalidate analyses in scientific papers, but it is difficult to track the papers impacted by bugs; (3) a software and its versions can disappear from the internet, making reliant scientific results challenging or impossible to reproduce. To solve these problems, I propose to develop PubSrc, an open-access resource to crawl scientific papers for computer code. Code, along with affiliated release dates and version information, will be made queryable independently from their original publication or website. PubSrc will thereby empower web searches to rapidly obtain lists of scientific papers that use software, by version and platform. Importantly, PubSrc will recognize that scripts and software themselves have software dependencies, and it will plot dependencies across papers, scripts, and software. In these ways, PubSrc will address problems (1) and (2). To address problem (3), PubSrc will automatically archive source codes of open-source software by crawling scientific papers. To achieve my goals, I plan on involving my PhD adviser Abhinav Nellore and former collaborators on PubCode, especially Ben Busby at NIH/NLM; the $25K will be used for travel and protected time to work on this project.</p>

<p>Prompt: What impact do you foresee from your efforts?</p>

<p>My Response: I anticipate the following positive developments from PubSrc: (1) Sets of scripts that were previously not recognized as independent units of scientific work (e.g., custom scripts for analyses accompanying a lab’s paper) will bear unique identities permitting (a) assessing their impact across the literature, (b) pinpointing bugs and other errors due to software dependencies, and (c) citability, whether or not these scripts were collected and assigned a DOI through other resources like, for example, Figshare. (2) Software developers who resolve bugs will be able to find and therefore apprise authors of scientific works whose results may be affected. Moreover, authors of scientific literature will be able to readily search for any bugs resolved since their papers came out. (3) Over time, results from papers that depend on scientific software will likely become more reproducible, independent of software availability outside PubSrc. We anticipate, in some cases, that code will not be open-source and only executables will be available. Indexing these may be permitted or limited by law. Further, some executables may depend on a particular computing environment for proper execution. A clear next step for PubSrc would then be to archive executables and/or make software containers available for reproduction of scientific results.</p>]]></content><author><name></name></author><category term="blog" /><category term="research" /><category term="bioinformatics" /><category term="replication" /><category term="compilation" /><category term="reproducibility" /><category term="scientific_code" /><category term="programming" /><category term="data_science" /><summary type="html"><![CDATA[How can we better preserve and track research software?]]></summary></entry></feed>